"use strict";

function GameStateManager() {
    var MODES = {
        GAME_SELECT: 0,
        TITLE_SCREEN: 1,
        CHARACTER_SELECT: 2,
        PLAY_GAME: 3
    };
    var mode = MODES.PLAY_GAME;
    var currentLevel = null;
    var player = null;

    function setMode(mode) {

    };
};